{
  "name": "server-upload",
  "version": "0.0.1",
  "description": "Upload the local file to the specified directory in the server",
  "bin": "./bin/upload.js",
  "main": "./bin/upload.js",
  "scripts": {
    "dev": "node bin/upload.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "findup-sync": "^4.0.0",
    "ssh2": "^0.8.6"
  }
}
